# CasaPosibleUSA

Sitio estático del proyecto "Casa Posible USA".

Contenido:

- `index.html`, `script.js`, `styles.css`
- Carpeta `archive/` con subproyectos
- `assets/` y otros recursos

Cómo usar localmente:

1. Abrir `index.html` en un navegador (sitio estático).
2. O servir con un servidor estático, por ejemplo:

```
python3 -m http.server 8000
```

Estado actual:

- Repositorio git inicializado localmente (si fue creado por el asistente).
- Aún no se ha creado un repositorio remoto en GitHub.

Próximos pasos sugeridos:

- Crear un repo en GitHub y hacer `git push` al remoto.
- Si quieres, puedo crear el repo remoto por ti (necesitaré acceso `gh`/token) o te doy los comandos.
